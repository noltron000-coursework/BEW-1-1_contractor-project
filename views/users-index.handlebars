<h1>Authors</h1>

<div class="row">
	<ul>
		<h2>
		{{#each users}}
			<li>
				<a href="/users/{{this.id}}">
					{{this.title}}
				</a>
				<p>
					<img src="https://image.tmdb.org/t/p/w500/{{this.poster_path}}" />
				</p>
			</li>
		{{/each}}
		</h2>
	</ul>
</div>